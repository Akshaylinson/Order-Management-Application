<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ splash screen overrides -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- System splash: white background so it blends with our splash image -->
        <!-- System splash: show Trinity logo as the center icon -->
        <!-- Transparent icon background so white splash_bg shows through -->
        <!-- Window background while Flutter initializes (after system splash) -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:navigationBarColor">#070707</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowSplashScreenBackground">#FFFFFF</item>
        <!-- White icon on white background = invisible, prevents launcher icon fallback -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon_transparent</item>
        <!-- White icon background ensures the circle container is also white -->
        <item name="android:windowSplashScreenIconBackgroundColor">#FFFFFF</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
        <item name="android:navigationBarColor">#070707</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
    <style name="SplashTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_bg</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/new_logo</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/splash_bg</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:navigationBarColor">#070707</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
